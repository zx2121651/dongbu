# YOLOv8 动画桌面捕捉软件 v3.0 (GUI版)

本项目是一个使用 `YOLOv8` 和 `Tkinter` 实现的单人和多人动画桌面捕捉软件。它能实时捕捉您的屏幕（全屏或指定区域），检测画面中的人物，并根据检测到的身体关键点（姿态）生成一个可定制、可平滑过渡的动画角色，最后将动画实时显示在一个带图形用户界面（GUI）的独立窗口中。

## ✨ 主要功能

- **图形用户界面 (GUI)**:
    - 使用 `Tkinter` 构建，提供一个独立的设置面板来控制所有主要功能。
    - 实时视频/动画预览窗口。
- **高性能捕捉与检测**:
    - **模型选择**: 允许用户在不同的YOLOv8姿态模型（n, s, m）之间切换，以在**速度和精度**之间取得平衡。
    - **指定区域捕捉 (ROI)**: 允许用户通过鼠标拖拽选择屏幕上的任意区域进行捕捉，极大地提升了性能和帧率。
- **高级动画效果**:
    - **高级平滑 (1-Euro Filter)**: 采用先进的一欧元滤波器，相比简单的线性平滑，能更有效地抑制噪声和抖动，同时在快速移动时保持低延迟，使动画更稳定、更跟手。
    - **可定制角色**: 通过GUI下拉菜单选择不同的角色外观（颜色、粗细）。
    - **动画预览**: 在设置面板中实时预览所选角色的外观。
- **灵活的视图模式**:
    - **动画视图**: 在纯色或自定义图片背景上显示动画角色。
    - **视频骨骼预览**: 直接在原始捕捉画面上叠加显示检测到的骨骼线条，方便调试和观察。
- **丰富的控制选项**:
    - **滤波器参数调整**: 通过GUI滑块微调一欧元滤波器的 `min_cutoff` 和 `beta` 参数，以获得最理想的平滑效果。
    - **背景切换**: 通过GUI下拉菜单选择纯色或自定义图片背景。
    - **阈值调整**: 通过GUI滑块动态调整姿态检测的置信度阈值。
    - **视频录制**: 通过GUI按钮方便地开始和停止录制，将动画保存为 `output.mp4`。

## 📂 项目结构

```
.
├── main_gui.py         # GUI应用程序主入口
├── capture_thread.py   # 后台捕捉与处理线程
├── drawing.py          # 绘图相关函数
├── config.py           # 静态配置文件
├── roi_selector.py     # 屏幕区域选择器UI
├── one_euro_filter.py  # 一欧元滤波器实现
├── requirements.txt    # 依赖库
└── README.md           # 本文档
```

## 📦 安装指南

1.  **克隆或下载项目**:
    将本项目所有文件下载到您的本地文件夹。

2.  **创建Python虚拟环境 (推荐)**:
    ```bash
    python -m venv venv
    source venv/bin/activate  # 在 Windows 上使用 `venv\Scripts\activate`
    ```

3.  **安装依赖**:
    ```bash
    pip install -r requirements.txt
    ```

## 🚀 使用方法

1.  **运行程序**:
    在您的终端或命令行中，进入项目文件夹，然后运行主程序：
    ```bash
    python main_gui.py
    ```
    程序启动后，会弹出一个带设置面板的窗口。后台线程会开始捕捉全屏画面并显示动画。

2.  **使用GUI设置面板**:
    窗口右侧的面板提供了对所有功能的控制：
    - **选择捕捉区域**: 点击后主窗口会隐藏，屏幕变暗，此时您可以按住鼠标左键拖拽出一个矩形区域。松开鼠标后，程序将只捕捉该区域。
    - **重置为全屏**: 点击后恢复对整个主显示器的捕捉。
    - **检测模型**: 选择一个YOLOv8模型。`n`最快但精度最低，`m`最慢但精度最高。切换模型后程序会自动重启后台线程。
    - **视图模式**: 选择“动画视图”或“视频骨骼预览”。
    - **角色外观**: 从下拉菜单选择不同的角色风格。
    - **背景**: 从下拉菜单选择不同的背景（仅在动画视图下有效）。
    - **置信度阈值**: 拖动滑块以过滤掉低置信度的检测结果。
    - **滤波器参数**: 微调 `min_cutoff` 和 `beta` 以改变平滑效果。较低的`min_cutoff`可以减少慢速移动时的抖动；较高的`beta`可以减少快速移动时的延迟。
    - **外观预览**: 实时显示当前选择的角色外观。
    - **开始/停止录制**: 点击按钮来开始或停止录制视频。

3.  **自定义背景 (可选)**:
    如果您想使用自己的图片作为背景，请将一张图片命名为 `background.jpg` 并放置在与 `main_gui.py` 相同的目录下。程序启动时会自动检测并将其加入到背景下拉菜单中。

## 📝 注意事项

- **性能**: 如果感觉卡顿，强烈建议使用“选择捕捉区域”功能并选择一个较小的区域，或者切换到更小的检测模型（如`yolo11n-pose.pt`）。
- **首次运行**: 首次运行或切换到一个新模型时，`ultralytics` 库会自动下载所需的模型文件。请确保您的网络连接正常。
